# TRD 输出模板

使用本模板组织并写入 FlowUS 中当前 EPIC 页面下的 `TRD` 页面。

这份模板是仓库内的写作辅助，不是最终落地文档。
最终权威内容必须写回当前 EPIC 页面下的 `TRD` 页面。

原则：

- 只写已经确认的技术规格
- 明确承接域级 `Solution` 和当前 EPIC 的 `PRD`
- `PRD` 中的关键规则和状态必须能在 `TRD` 中找到技术落点
- 未决问题统一进入当前业务域根页面下的页面 `待确认事项`
- 需要暂时保留但尚未验证的判断写入 `关键假设`
- 被明确放弃的路径写入 `已放弃 / 暂缓方案`
- `PRD -> TRD` 映射必须稳定可追溯

---

````markdown
# 技术设计文档（TRD）：{EPIC 名称}

## 当前共识

- 当前已经确认的 3-7 个技术结论
- 本 EPIC 当前最重要的技术边界
- 当前最关键的模型或实现方向

## 一、模块规划

### 1.1 范围确认

**本 TRD 实现：**

- {能力}

**本 TRD 不实现：**

- {不在范围内的内容}

### 1.2 模块拆分

| 模块 | 技术名 | 职责 | 类型 | 依赖 |
|------|--------|------|------|------|
| {模块} | {技术名} | {职责} | 核心 / 桥接 / 扩展 | {依赖} |

### 1.3 manifest 规格

| 字段 | 值 |
|------|---|
| name | {值} |
| version | {值} |
| depends | {值} |
| data | {值} |

### 1.4 模块依赖图

```mermaid
graph TD
    A[{模块A}] --> B[{模块B}]
```

### 1.5 模型关系图

```mermaid
classDiagram
    class 模型A["{模型A}"] {
        <<新建 / 继承扩展 / 原生>>
        {字段}
        --
        {方法}
    }
```

### 1.6 文件结构

```text
{module_name}/
├── __manifest__.py
├── models/
├── views/
├── security/
├── data/
└── tests/
```

## 二、模型定义

### 2.1 模型来源对照表

| 模型 | 技术名 | 来源 | 改动方式 | 改动内容 |
|------|--------|------|----------|----------|
| {模型} | {技术名} | 原生 / 新建 | _inherit / _name / 不改 | {说明} |

### 2.2 模型明细

#### {技术模型名}

- `_name / _inherit = ...`
- `_description = ...`
- `_order = ...`

| 字段名 | 类型 | 参数 | 说明 |
|--------|------|------|------|
| {字段} | {类型} | {参数} | {说明} |

| 关系字段 | 关联模型 | 关系类型 | 说明 |
|----------|----------|----------|------|
| {字段} | {模型} | Many2one / One2many / Many2many | {说明} |

## 三、PRD -> TRD 映射

| PRD 规则 | PRD 位置 | 实现方式 | TRD 方法 / 文件 |
|----------|----------|----------|-----------------|
| {规则} | {位置} | {实现方式} | {方法 / 文件} |

## 四、业务逻辑实现

### 4.1 校验规则实现

| 方法名 | 模型 | 实现方式 | PRD 规则 | 逻辑 |
|--------|------|----------|----------|------|
| {方法} | {模型} | {方式} | {规则} | {逻辑} |

### 4.2 自动计算实现

| 方法名 | 模型 | 装饰器 | 依赖字段 | 逻辑 | PRD 规则 |
|--------|------|--------|----------|------|----------|
| {方法} | {模型} | {装饰器} | {字段} | {逻辑} | {规则} |

### 4.3 工作流实现

| 方法名 | 触发方式 | PRD 转换 | 前置检查 | 状态变更 | 系统动作 |
|--------|----------|----------|----------|----------|----------|
| {方法} | {方式} | {转换} | {检查} | {状态} | {动作} |

### 4.4 自动化动作实现

| 实现 | 类型 | PRD 规则 | 说明 |
|------|------|----------|------|
| {实现} | {类型} | {规则} | {说明} |

### 4.5 编号 / 命名规则实现

| 对象 | 实现方式 | 说明 |
|------|----------|------|
| {对象} | {方式} | {说明} |

## 五、权限

### 5.1 角色组

| 组名 | 技术名 | 隶属 | 说明 |
|------|--------|------|------|
| {组名} | {技术名} | {隶属} | {说明} |

### 5.2 访问权限

| id | model | group | 读 | 写 | 建 | 删 |
|----|-------|-------|----|----|----|----|
| {id} | {model} | {group} | ✅ | ✅ | ✅ | ❌ |

### 5.3 记录规则

| 规则名 | 模型 | 适用组 | domain | 说明 |
|--------|------|--------|--------|------|
| {规则} | {模型} | {组} | {domain} | {说明} |

## 六、视图实现

### 6.1 视图文件规划

| 文件 | 作用 | 说明 |
|------|------|------|
| {文件} | {作用} | {说明} |

### 6.2 关键视图技术说明

| 区域 | 实现 |
|------|------|
| {区域} | {实现} |

### 6.3 继承视图 / 动作 / context（如涉及）

| xpath / 动作 | 操作 | 内容 / 说明 |
|--------------|------|-------------|
| {xpath} | {操作} | {内容} |

## 七、数据文件

### 7.1 序号规则

| 字段 | 值 |
|------|---|
| name | {值} |
| code | {值} |

### 7.2 通知模板

| 字段 | 值 |
|------|---|
| name | {值} |
| model_id | {值} |
| 触发时机 | {值} |

### 7.3 定时任务

| 字段 | 值 |
|------|---|
| name | {值} |
| code | {值} |
| interval | {值} |

## 八、测试指引

| 测试类别 | 场景 | 验证点 |
|----------|------|--------|
| {类别} | {场景} | {验证点} |

## 九、外部接口（按需）

| 项目 | 说明 |
|------|------|
| 端点 | {值} |
| 认证 | {值} |

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {字段} | {类型} | ✅ / ❌ | {说明} |

## 关键假设

- {暂时接受但尚未验证的判断}

## 已放弃 / 暂缓方案

- {明确讨论过但当前不采用的方案}

## 下一步重点确认

- {下一次最值得推进或验证的点}
````
