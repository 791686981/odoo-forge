---
title: "The button's text if there is no `icon`, the `alt` text for the icon otherwise."
source_url: "https://docs.odoo.sbggai.top/developer/reference/user_interface/view_architectures/button_attribute_string.html"
---

.. attribute:: string
   :noindex:

   The button's text if there is no `icon`, the `alt` text for the icon otherwise.

   .. example::
      ```xml

         <button type="object" name="action_create_new" string="Create document"/>

   :requirement: Optional
   :type: str
   :default: `''`
